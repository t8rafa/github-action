name: Generate feature candidate

on:
  workflow_dispatch:
    inputs:
      zappts_approve:
        description: 'Zappts approved?'
        required: true
        default: 'false'


jobs:
  teste:
    needs: zappts
    runs-on: ubuntu-latest
    steps:
      - if: github.event.inputs.zappts_approve == 'true'
        name: Creating feature approved
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

#Pegar valor da branch que foi acionada na trigger, dar checkou nela e criar a partir dela a nova. 
#Realizar estágio onde valida se está ok no environemnts anteriores 
# Só realizar algo se a branch for igual feature/**



##deletar versão publicada em dev, qa e zappts
#      "${{ github.event.inputs.message }}
#      github.event.inputs.use-emoji == 'true' 
#      ${{ github.event.inputs.name }}"
